export const REFINEMENT_LOOP_PROMPT = `SupremeAppsBuilder refinement loop:
1. generate
2. validate
3. benchmark smoke quality
4. review failing gates
5. apply deterministic fixes
6. stop when all blocking gates pass or the refinement budget is exhausted`;
